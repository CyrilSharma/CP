#include <bits/stdc++.h>
using namespace std;
int main() {
    int n, q;
    cin >> n >> q;
    // each start has a stopping point.
    // sort starts. binary search...
    // basically timestep tells you current rate, and current amount,
    // very quick lookup.
}